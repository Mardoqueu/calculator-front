import { SignIn } from "../components/SignIn";

/**
 * Renders the login page component.
 *
 * @return {JSX.Element} The rendered JSX.Element that includes the sign-in component.
 */
export function LoginPage() {
  return (
    <>
      <SignIn />
    </>
  );
}
