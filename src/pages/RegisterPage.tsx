import { SignUp } from "../components/SignUp";

/**
 * Represents a registration page component.
 * This component renders a sign-up section where users can create an account.
 *
 * @return {JSX.Element} The JSX representation of the sign-up page.
 */
export function RegisterPage() {
  return (
    <>
      <SignUp />
    </>
  );
}
